services:
  superapp-apec:
    container_name: superapp-apec
    image: supperapp-apec
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "127.0.0.1:3010:3010"

    restart: unless-stopped

    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3010"]
      interval: 30s
      timeout: 5s
      retries: 3

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
